[user]
	name = {{ git_name }}
	email = {{ git_email }}
[core]
	excludesfile = /home/{{ user }}/.gitignore_global
[push]
	default = simple
[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	last = log -1 HEAD
	stashq = -c commit.gpgsign=false stash
