TODO

install tor, pgp etc.

sqlite
phantomjs
crystal lang
Java
Clojure

FIXME: The MySQL root password is not blank. I had to set it as such:
sudo mysqld_safe --skip-grants-tables
mysql -uroot
  use mysql;
  update user set authentication_string=password('') where user='root';
